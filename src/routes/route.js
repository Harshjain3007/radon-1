const express = require('express');
const externelmodule = require('../routes/route.js')
//const externelmodule = require('../util/helper')
//const externelmodule = require('../validator/formater')


const router = express.Router();

router.get('/test-me', function(req, res) {

});